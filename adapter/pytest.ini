# Test framework: pytest with auto mode for asyncio
# Run: cd adapter && uv run pytest -v
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = integration: spawns the real reasonix binary (slow, requires built bin)
